services:
  # Frontend Static Site
  - type: static
    name: CMS
    buildCommand: npm install && npm run build
    staticPublishPath: ./dist
    domain: codewithmrsingh.me
    headers:
      - path: /*
        name: Cache-Control
        value: public, max-age=31536000, immutable
    routes:
      - type: rewrite
        source: /
        destination: /seo-index.html
      - type: rewrite
        source: /*
        destination: /index.html
    envVars:
      - key: NODE_ENV
        value: production

  # Backend API Service
  - type: web
    name: CMS_backend
    env: node
    region: oregon
    buildCommand: |
      cd server && npm install
    startCommand: |
      cd server && npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: EMAIL_USER
        sync: false  # encrypted - your Gmail address
      - key: EMAIL_PASS
        sync: false  # encrypted - your Gmail app password
      - key: CLIENT_URL
        value: https://cms.codewithmrsingh.me
    autoDeploy: true
    healthCheckPath: /health